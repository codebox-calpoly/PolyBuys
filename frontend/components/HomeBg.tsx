/**
 * HomeBg.tsx — native stub.
 * On iOS/Android the background is handled by the page backgroundColor.
 * This file exists so the import resolves on all platforms.
 */
export function HomeBg() {
  return null;
}
