/**
 * AppNav.tsx — native stub.
 * On iOS/Android the tab bar is handled by NativeTabs in (tabs)/_layout.tsx.
 * This file exists so the import resolves on all platforms.
 */
export function AppNavContainer() {
  return null;
}
