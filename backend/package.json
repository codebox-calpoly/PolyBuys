{
  "name": "@polybuys/backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "convex dev",
    "build": "npm run build:functions",
    "build:functions": "tsc",
    "typecheck": "tsc --noEmit",
    "deploy": "convex deploy"
  },
  "dependencies": {
    "convex": "^1.17.2",
    "@polybuys/shared": "*",
    "@convex-dev/auth": "^0.2.0",
    "@sendgrid/mail": "^8.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  },
  "private": true
}
